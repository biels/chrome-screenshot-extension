{
  "name": "chrome-screenshot-extension",
  "version": "1.0.0",
  "description": "Capture screenshots of the current page",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && cp src/*.{html,json,png} dist/"
  },
  "author": "Biel Simon",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/chibat/chrome-extension-typescript-starter.git"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/chrome": "0.0.188",
    "typescript": "^4.4.3"
  }
}
